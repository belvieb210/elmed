{
  "name": "matemedical",
  "private": true,
  "description": "MateMedical — ERP médical B2B",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "db:preparer": "npm run db:preparer --prefix backend",
    "installer": "npm install --prefix backend && npm install --prefix frontend",
    "build:prod": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
